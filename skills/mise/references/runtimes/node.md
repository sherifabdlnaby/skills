# Node (runtime integration)

## Rules and Best Practices:

## Notes & Gotchas:

1. Corepack's Yarn can override mise's install if yarn is defined below node in the tools. If you want to manage Yarn (or pnpm) make sure to have Yarn above Node in tools.
2. Always include packageManager in package.json even if packageManager is managed by mise.

## Syntax

wip
