description: TODO one line on when to use this command. Shown in the command list.
argument-hint: "[arg]"
---

TODO: replace this body with the command's prompt.

Whatever the user types after `/example` is available as `$ARGUMENTS`.
