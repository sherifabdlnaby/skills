# Agent instructions for the `mise-fy` skill

- `SKILL.md` is a router: keep the `## Router` entries in sync with the `references/*.md` files — one entry per file, a one-line list of what it covers, no procedure text (procedures live in the reference).
- Before planning or editing mise/hk/CI config, read the matching `references/` file (and `assets/` when copying a scaffold). SKILL.md alone is not enough.
- After editing any `.md` file in this skill, run `rumdl check skills/mise-fy/<file>` and fix what it flags.
